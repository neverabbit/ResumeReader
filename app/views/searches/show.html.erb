<div class="row">
	
	<div class="col-md-3 col-md-offset-1">
		<%= render 'searches/search_field' %>
	</div>
	
	<div class="col-md-7">
		<br>
		符合条件的共有 <%= @resumes.count %> 人
		<%= will_paginate @resumes %>
		

		<%= render @resumes %>
		
		<%= will_paginate @resumes %>
	</div>
</div>
